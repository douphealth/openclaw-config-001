# Bootstrap

1. Provision VPS.
2. Install Node 22+.
3. Install OpenClaw.
4. Run `openclaw onboard --install-daemon`.
5. Clone this repo.
6. Restore secrets from vault.
7. Run `make validate` and `make health`.
