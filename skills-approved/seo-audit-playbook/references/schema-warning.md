# Schema Warning

Static fetch methods are not enough for reliable schema detection.

If the audit needs a schema finding:
- prefer browser-rendered inspection
- or use trusted rendered validators
- avoid definitive “missing schema” claims from stripped HTML alone
