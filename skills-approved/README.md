# skills-approved

This directory is the curation layer for production-approved skills.

Current repo mode is transitional:
- approved skill paths are declared in `MANIFEST.yaml`
- source skill implementations currently remain in legacy `skills/`
- this directory will eventually hold the fully migrated production set
