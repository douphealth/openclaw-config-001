# Request Optimization Pipeline

1) Local-first bypass
2) Cache lookup
3) Dedup/in-flight check
4) Batch eligibility check
5) Model route by complexity + budget
6) Prompt compression + schema output
7) Budget preflight
8) Execute + retry/fallback
9) Update ledger + cache + limiter state
