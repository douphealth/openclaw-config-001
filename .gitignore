# === CREDENTIALS — NEVER COMMIT ===
.secrets/
*.env
**/vault/
**/.env.*

# === RUNTIME STATE (regenerated each session) ===
core/STATUS.md
world-model/**/*.json

# === BUILD/CACHE ARTIFACTS ===
__pycache__/
*.pyc
.venv/
node_modules/
.DS_Store
Thumbs.db

# === LARGE EPISODIC LOGS (archive separately after 90 days) ===
# memory/*.md IS tracked — prune manually
